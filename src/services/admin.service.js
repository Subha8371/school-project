// import axiosIntercepterCall from ".";

// const getAdminDetails = async (req, res) => {
//   try {
//     // Call the axiosIntercepterCall to make the API request
//     const response = await axiosIntercepterCall("/admin");
//     // Assuming the admin details are at the "/admin" route
//     const adminDetails = response.data.data.list;
//     return adminDetails;
//   } catch (error) {
//     // Handle the error appropriately
//     console.error("Error fetching admin details:", error);
//   }
// };

// export default getAdminDetails;

// // store -> slice -> createGroupSlice.js -> const { addGroup, fetchGroup } = useSeletor();
// // 1. code review
// // 2. redux toolkit
// // 3. service+redux data fetching process
